export default function Home() {
  return (
    <div>
      <h1>WhatsApp Bot API</h1>
      <p>Use the /api/enviar endpoint to start WhatsApp client and generate QR code on the server console.</p>
    </div>
  );
}
